{
  "name": "replo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel --filter @replo/api --filter @replo/web dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "smoke": "tsx scripts/smoke.ts",
    "smoke:live": "tsx scripts/smoke-live.ts",
    "verify:esp": "tsx scripts/verify-esp-connection.ts"
  },
  "devDependencies": { "@types/node": "^24.0.0", "tsx": "^4.20.0", "typescript": "^5.8.0", "vitest": "^3.2.0" }
  ,"dependencies": { "next": "^15.4.0", "react": "^19.1.0", "react-dom": "^19.1.0" }
}
