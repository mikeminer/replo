================================================================================
PASTE THIS AS THE FIRST MESSAGE TO CHATGPT CODEX
================================================================================

Read and fully execute CODEX_MASTER_PROMPT.md and AGENTS.md end-to-end.

Build the COMPLETE Replo product (not a demo stub) in this repo.

CRITICAL — REAL DELIVERABILITY DAY 1:
- Implement a FULL Smartlead API integration (preferred) or full Instantly integration in packages/sending.
- Production path SEND_PROVIDER=smartlead must create campaigns, push leads, set sequences, start sending REAL email, and ingest REAL reply + bounce webhooks.
- Free tier: limited REAL sends from Replo-managed mailboxes; reply CONTENT redacted server-side until Pro monthly subscription.
- Mock sender is ONLY for CI when keys are missing — shipping simulate-only as the product is FAILURE.
- Write docs/DELIVERABILITY_RUNBOOK.md (DNS SPF/DKIM/DMARC, warmup, webhooks, ramp).
- Implement scripts/verify-esp-connection.ts and pnpm smoke:live (only to LIVE_TEST_LEAD_EMAIL you control).

Also required:
1) apps/api = replo.eu platform; apps/web = replo.it consuming API only via SDK/HTTP.
2) ZERO third-party email FINDERS (no Hunter/LeadMagic/Findymail/Apollo find). Proprietary packages/enrichment only.
3) Stripe Pro monthly (or BILLING_MOCK) unlocks full replies + respond + book.
4) Phases 0→5 without stopping. docs/PROGRESS.md updates. Finish when pnpm test, build, smoke pass AND real ESP client is complete. docs/SHIP_REPORT.md must include "Real deliverability Day 1".

Start Phase 0 now. No confirmation between phases. Never add email broker finders. Never replace real ESP with simulate-only architecture.

================================================================================
